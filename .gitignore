node_modules
.next
.next-dev
.env*.local
next-env.d.ts
*.tsbuildinfo
.vercel
.DS_Store

# Supabase CLI scratch. Migrations and config.toml ARE committed; these are not.
supabase/.temp/
supabase/.branches/

# One-off local file holding the first-login password. Not for git.
FIRST-LOGIN.local.md

# Written by the Sentry build plugin when it uploads source maps.
.env.sentry-build-plugin

# Photographs and clips built by scripts/demo-media.mjs. Derived, not source —
# the recipe is in scripts/demo-dataset.mjs and rebuilds them on demand.
.demo-media/
