{
  "name": "takemore",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=@takemore/web",
    "build": "npm run build --workspace=@takemore/web",
    "start": "npm run start --workspace=@takemore/web"
  }
}
