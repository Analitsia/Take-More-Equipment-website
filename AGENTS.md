# Agent instructions

The instructions for this repo live in **[CLAUDE.md](CLAUDE.md)**. Read that file first — it is
the single source of truth for how to work here, for every AI tool.

Do not duplicate rules into this file. If something needs to change, change `CLAUDE.md`.
